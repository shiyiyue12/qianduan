# 02

## 表格元素

表格主要用来展示数据
表格元素的容器使用table标签
其他表格元素的标签：thead，tbody，th，tr，td等

## 表单元素

表单元素容器使用form标签
常用的标签包括有：
input:type = "txt"
input:type = "password"
input:type = "radio"
input:type = "checkout"
input:type = "button"
input:type = "submit"
label
select
option
